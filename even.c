#include<stdio.h>

int main() {
    int budget=0;
    int count=0;

    for(int i=0;i<=100;i++) {
        if(i%2==0) {
            count++;
            

        }
    }
    printf("total budget is %d\a", count*200);
    return 0;
}